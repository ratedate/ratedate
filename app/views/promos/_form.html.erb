<%= form_with(model: promo, local: true) do |form| %>
  <% if promo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promo.errors.count, "error") %> prohibited this promo from being saved:</h2>

      <ul>
      <% promo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :string %>
    <%= form.text_field :string, id: :promo_string %>
  </div>
  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :promo_number %>
  </div>
  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :promo_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
