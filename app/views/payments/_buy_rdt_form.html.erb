<%= form_with(url: payment_path, method: :post, local: true) do |form| %>
    <%= form.number_field :rdt_amount, id: 'rdt_amount', min: '1' %>
    <%= form.submit 'Buy RDT', class: 'round-btn' %>
<% end %>